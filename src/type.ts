export interface messagingProps {
  avatar: string;
  name: string;
  id: string
}
export interface chatProps {
  name: string;
  msg: string;
  color: string;
  senderId: string;
  dateSent: number;
}